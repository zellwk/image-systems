---
// Shown in place of the video demos when ffmpeg isn't available,
// e.g. inside StackBlitz's webcontainer.
---

<div class="card vertical gap-2">
  <h3>This demo needs ffmpeg</h3>
  <p>
    The <code>Video</code> component reads each video's dimensions with
    ffmpeg — which StackBlitz can't run. Download the code and run it locally
    to see this page in action:
  </p>
  <pre><code>git clone https://github.com/zellwk/image-systems
cd image-systems
npm install
npm run dev</code></pre>
</div>
